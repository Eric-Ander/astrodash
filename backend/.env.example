# AstroDash Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
PORT=3000
NODE_ENV=production

# Application URL (used in email links)
APP_URL=https://your-domain.com

# =============================================================================
# OPENWEATHERMAP API
# =============================================================================
# Get your free API key at: https://openweathermap.org/api
# Free tier: 1,000 calls/day (more than enough)
OPENWEATHER_API_KEY=your_api_key_here

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DATABASE_PATH=./data/astroweather.db

# =============================================================================
# CACHE CONFIGURATION
# =============================================================================
# Cache duration in minutes (default: 60)
CACHE_DURATION=60

# =============================================================================
# AUTHENTICATION
# =============================================================================
# JWT secret key for signing tokens (use a long random string)
JWT_SECRET=your-secure-secret-key-change-this-in-production

# =============================================================================
# EMAIL NOTIFICATIONS (SMTP)
# =============================================================================
# Email notifications are optional. Leave these empty to disable.
# Users can enable notifications for their saved locations to receive
# alerts when conditions are good for stargazing.

# SMTP Server Configuration
# Examples:
#   Gmail: smtp.gmail.com (port 587)
#   Outlook: smtp.office365.com (port 587)
#   SendGrid: smtp.sendgrid.net (port 587)
#   Mailgun: smtp.mailgun.org (port 587)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SMTP Authentication
# For Gmail, use an "App Password" (not your regular password)
# https://support.google.com/accounts/answer/185833
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Use TLS/SSL (false for port 587, true for port 465)
SMTP_SECURE=false

# From address for emails (optional, defaults to SMTP_USER)
SMTP_FROM=noreply@your-domain.com

# =============================================================================
# NOTIFICATION SCHEDULER
# =============================================================================
# Cron schedule for checking and sending notifications
# Default: "0 14 * * *" (daily at 2 PM - gives users time to plan for evening)
# Format: minute hour day month weekday
# Examples:
#   "0 14 * * *"  = Daily at 2:00 PM
#   "0 16 * * *"  = Daily at 4:00 PM
#   "0 12,16 * * *" = Daily at 12:00 PM and 4:00 PM
NOTIFICATION_SCHEDULE=0 14 * * *
